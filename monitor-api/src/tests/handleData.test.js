const handleData = require("../handleData");

//Not finished writing test cases for this module yet
console.log(
  "Not finished writing test cases for this module yet, Dont consider below results"
);
describe("handleData", () => {
  describe("saveData", () => {
    it("should save data to file", () => {
      expect(1).toBe(1);
    });
  });
  describe("getData", () => {
    it("should get data from file", () => {
      expect(1).toBe(1);
    });
  });
});
